// const elements = Array.from(document.querySelectorAll(".nav__list-el"));
// console.log(elements);
//
// let lastEl = elements.find((el, index) => index === elements.length-1);
// let firstEl = elements.find((el, index) => index === 0);
// console.log(firstEl.innerHTML)

